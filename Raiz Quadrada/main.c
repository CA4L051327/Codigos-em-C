#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
  float a=5, b=10, c=-20;

            float r1 = ( -b+ (sqrt(b*b-4*a*c)));      (2*a) ;
            float r2 = ( -b- (sqrt(b*b-4*a*c)));       (2*a) ;

        printf ("Raiz 1  =  %f\nRaiz  2   =  %f", r1,  r2);

            return 0;
}
